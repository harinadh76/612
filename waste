<form [formGroup]="reactiveForm" (ngSubmit)="registerLoginUser()">
    <ng-template [ngIf]="pageValue == 'login'">
        <div class="login-box">
            <h2>Login</h2>
            <form>
              <div class="user-box">
                <input type="text"formControlName="userName">
                <label>Username</label>
              </div>
              <!-- <div class="user-box">
                <input type="password" formControlName="password">
                <label>Password</label>
              </div> -->
              <a href="#">
                <span></span>
                <span></span>
                <span></span>
                <span></span>
                Submit
              </a>
            </form>
          </div> 
    </ng-template>
    
    <ng-template [ngIf]="pageValue == 'register'">
        <div class="login-box">
            <h2>Register</h2>
            <form>
              <div class="user-box">
                <input type="text" formControlName="userName">
                <label>Username</label>
              </div>
              <!-- //dob county gender -->
              <!-- <div class="user-box" formControlName="email">
                <input type="email" id="email">
                <label>Email</label>
              </div> -->
              <!-- <div class="user-box" formControlName="password">
                <input type="password">
                <label>Password</label>
              </div> -->
              <a href="#" >
                <span></span>
                <span></span>
                <span></span>
                <span></span>
                <!-- <button>Submit</button> -->
                Submit
              </a>
              <input type="submit">
            </form>
          </div> 
    </ng-template>
</form>
